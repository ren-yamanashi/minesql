# リーフノード

- 構造

  ```txt
  |ノードタイプヘッダー|リーフノードヘッダー|Slotted Page  |
  ↑____8 bytes_____↑____16 bytes____↑__4072 bytes__↑
  ```

  - 合計 4096 byte (1 ページのサイズ)
  - ノードタイプヘッダー: 8 byte (`"LEAF    "`)
  - リーフノードヘッダー: 16 byte (前 PageId + 次 PageId)
  - Slotted Page: 4072 byte

- リーフノードのデータ構造は Slotted Page になっているので、データの挿入などは [Slotted Page](./slotted-page.md) の仕様に従う
- Slotted Page のスロット数 = key-value ペアの数

## リーフノードヘッダーの構成

- サイズ: 16 byte
  - 前のリーフノード (=前のページ) へのポインタ: 8 byte
    - 前のリーフノードの PageId を格納する
    - PageId は FileId (4 byte) + PageNumber (4 byte) で構成される
  - 次のリーフノード (=次のページ) へのポインタ: 8 byte
    - 次のリーフノードの PageId を格納する
    - PageId は FileId (4 byte) + PageNumber (4 byte) で構成される

前のページへのポインタを読み取る際には、ページデータの 8 byte 目から 8 byte を読み取る  
同様に、次のページへのポインタは 16 byte 目から 8 byte を読み取る

書き込みの際も同様に、8 byte 目から 8 byte に前の PageId を、16 byte 目から 8 byte に次の PageId を書き込む

## リーフノードに key-value ペアを挿入

- リーフノードが満杯でない場合
  - Slotted Page の仕様に従って、リーフノードのボディ部分に key-value ペアを挿入する

- リーフノードが満杯の場合
  - 新しいリーフノードを作成し、自身のリーフノードの key-value ペアのうち、半分を新しいリーフノードに移動させる
  - 新しいリーフノードに、挿入対象の key-value ペアを挿入する

※実装コード的には、`Insert` 時にリーフノードが満杯であったら、そのことを呼び出し元に伝える方針にしている (内部的に新しいリーフノードを作成し、分割挿入する処理 (`SplitInsert`) は行なっていない).  
これは、LeafNode (struct) を、自分のページ内のデータ操作だけに集中させるための設計上の判断である.

### 分割挿入の流れ

#### 1. 分割挿入先のリーフノードを作成する

#### 2. 古いノードのペアを新しいノードに移動しつつ、挿入対象のペアを適切な位置に挿入する

- 挿入対象の key-value ペアのキーと、分割前のリーフノード (古いノード) の先頭の key を比較する
  - 古いノードの先頭ペアのキー < 挿入対象のキー: 古いノードの先頭ペアを分割先のリーフノード (新しいノード) に移動する
    - その過程で、新しいノードが半分埋まったら、挿入対象の key-value ペアを古いノードに挿入して、ループ終了
  - 古いノードの先頭ペアのキー >= 挿入対象のキー: 挿入対象の key-value ペアを新しいノードに挿入する
    - その後、新しいノードが半分埋まるまで、古いノードの先頭ペアを新しいノードに移動する

**_以下例_**

```md
_例1: 挿入対象のキーが中間にある場合_

初期状態:
  - 古いノード (満杯): [10, 20, 30, 40, 50]  ← newPair = 25 を挿入したい
  - 新しいノード (空):  []

ループ 1回目:
  - 新しいノードが半分埋まっているか? → No (空なので)
  - 古いノードの先頭(10) < 挿入対象(25) → 古いノードの先頭ペア(10)を新しいノードに移動
  - 結果
    - 古いノード: [20, 30, 40, 50]
    - 新しいノード: [10]

ループ 2回目:
  - 新しいノードが半分埋まっているか? → No (まだ半分未満)
  - 古いノードの先頭(20) < 挿入対象(25) → 古いノードの先頭ペア(20)を新しいノードに移動
  - 結果
    - 古いノード: [30, 40, 50]
    - 新しいノード: [10, 20]

ループ 3回目:
  - 新しいノードが半分埋まっているか? → No (まだ半分未満)
  - 古いノードの先頭(30) > 挿入対象(25) → 挿入対象(25)を新しいノードに挿入
  - その後、新しいノードが半分埋まるまで古いノードの先頭ペアを移動する
    - 新しいノード [10, 20, 25] → 半分埋まったので移動終了
  - 結果
    - 古いノード: [30, 40, 50]
    - 新しいノード: [10, 20, 25]

_例2: 挿入対象のキーが先頭より小さい場合_

初期状態:
  - 古いノード (満杯): [10, 20, 30, 40, 50]  ← newPair = 5 を挿入したい
  - 新しいノード (空):  []

ループ 1回目:
  - 新しいノードが半分埋まっているか? → No (空なので)
  - 古いノードの先頭(10) >= 挿入対象(5) → 挿入対象(5)を新しいノードに挿入
  - その後、新しいノードが半分埋まるまで古いノードの先頭ペアを移動する
    - 1回目の移動: 古いノードの先頭(10)を新しいノードに移動
      - 古いノード: [20, 30, 40, 50], 新しいノード: [5, 10]
    - 2回目の移動: 古いノードの先頭(20)を新しいノードに移動
      - 古いノード: [30, 40, 50], 新しいノード: [5, 10, 20]
    - 新しいノードが半分埋まったので、移動終了
  - 結果
    - 古いノード: [30, 40, 50]
    - 新しいノード: [5, 10, 20]
```

## リーフノード内の特定の key-value ペアの value を更新

- 更新前後で key の値は変わらないと仮定する
- value のリサイズを行う
  - リサイズの仕組みは [Slotted Page の仕様](./slotted-page.md#データのサイズ変更手順)に従う
