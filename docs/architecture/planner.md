# プランナー

- 実装コード:

## 概要

- プランナーは SQL クエリを解析し、実行計画を立てる
  - 実行計画の通りにエグゼキュータを呼び出し、クエリを実行する
- 実行計画はツリー構造になっており、各ノードがエグゼキュータを表す

### メモ

- AST を見てバリデーションを行える部分 (例: `CREATE TABLE` で指定されたインデックスのカラムが、テーブルのカラムとして指定されているかどうか) は planner で行う
